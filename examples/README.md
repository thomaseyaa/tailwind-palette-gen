# Examples

Sample outputs produced by `tailwind-palette-gen`.

| File | Command |
|---|---|
| `brand.tokens.json` | `tailwind-palette-gen "#326dff" --name brand --format dtcg` |

These files are not regenerated automatically — they are committed snapshots
to make it easier to inspect the structure of each format without running the
CLI.
